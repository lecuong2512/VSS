{
  "name": "vss-chat-login",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "compile:css": "sass scss/style.scss css/style.css --no-source-map",
    "watch:css": "sass --watch scss/style.scss css/style.css --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.83.0",
    "vite": "^6.0.0"
  }
}
